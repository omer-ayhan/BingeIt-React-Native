import { StyleSheet } from "react-native";

import colors from "../../style/colors";

export default StyleSheet.create({
  commentTitle: {
    color: colors.dark,
    marginTop: 10,
    marginBottom: 5,
    fontSize: 16,
    fontWeight: "bold",
  },
  comment: {
    color: colors.dark,
  },
});
